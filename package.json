{
  "name": "log-viewer-app",
  "version": "1.0.0",
  "description": "INSTRUCTIONS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "start": "concurrently \"npm run dev --prefix express_app\" \"npm run dev --prefix react_app\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.0",
    "prettier": "^3.6.2"
  }
}
